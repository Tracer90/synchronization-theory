# Для команды — мысль, уточнение, суть

## Что мы сделали

**МЫСЛЬ**
Собрали персонализированный инструмент HRV-синхронизации через камеру смартфона, с самообучающимся ядром (IAC) и LLM-ассистентом.

**УТОЧНЕНИЕ**
- 46 файлов, ~8100 строк Dart
- IAC — 16 файлов, Self-Consistent Engine (14 шагов), Guard, Negative/Positive Intelligence, Somatic Engine, Diagrammatic Mode
- LLM — гибрид: локально через llama_cpp_dart или cloud через OpenAI API
- Тесты — 6 файлов, ~1450 строк

**СУТЬ**
Вся кодовая база в `C:\Projects\FREE_EVER\synctapp\lib\`. Запуск: `flutter run`. Без Flutter SDK — только чтение и анализ.

---

## Аудит (INV-033 Schizodetail)

**МЫСЛЬ**
Проверили каждый файл на 5 уровнях: синтаксис, типы, граничные случаи, инварианты, состояние до волны.

**УТОЧНЕНИЕ**
Нашли и исправили 6 багов:
1. **CRITICAL** — `IACEngine._model` рассинхронизирован с `IACNotifier.state` → потеря правил
2. **HIGH** — TOCTOU в `_somaticEngine.snapshots.last` → race condition
3. **HIGH** — NaN/Infinity в JSON → `jsonEncode` падает
4. **HIGH** — Guard делает `as double` без проверки → runtime ошибка
5. **MEDIUM** — `hashCode.toDouble()` как данные → бессмысленно
6. **MEDIUM** — `SomaticSnapshot` без `copyWith` → хрупкий код

**СУТЬ**
Исправлено всё. Добавлено 14 тестов на граничные случаи. Система готова к бета-тесту.

---

## Что делать дальше

**МЫСЛЬ**
Три параллельных трека: бета-тест, LLM-интеграция, документация.

**УТОЧНЕНИЕ**
1. **Бета-тест** (см. BETA_TEST_PLAN.md):
   - Этап 0: BreathTuner + HRV4Biofeedback (3 дня самопроверка)
   - Этап 1: 3-5 человек, техническая верификация (1 нед)
   - Этап 2: 10-20 человек, функциональный тест (2 нед)
   - Этап 3: IAC-верификация (2 нед)

2. **LLM**:
   - cloud-бэкенд работает сразу (нужен API-ключ)
   - local-бэкенд требует GGUF модель (~2-4GB)
   - без LLM — встроенные эвристики (анализ RMSSD)

3. **Технический долг**:
   - Перенести `IACPersistence` на sembast
   - Добавить полную JSON-сериализацию для restore
   - Гранулярные тесты для каждого из 14 шагов IAC

**СУТЬ**
Код готов. Нужны люди с телефонами (Android/iOS) и Flutter SDK для сборки.

---

## Структура проекта (шпаргалка)

```
synctapp/
├── lib/
│   ├── core/
│   │   ├── iac/          # IAC ядро (16 файлов)
│   │   ├── llm/          # LLM (8 файлов)
│   │   ├── ppg/          # PPG-камера
│   │   ├── hrv/          # HRV-расчёт
│   │   ├── breathing/    # Дыхательный тренер
│   │   ├── rf_protocol/  # Поиск резонансной частоты
│   │   ├── adaptation/   # Тест архетипов
│   │   └── storage/      # Хранение данных
│   ├── features/
│   │   ├── measurement/  # Экран замера пульса
│   │   ├── practice/     # Экран практики
│   │   ├── rf_test/      # Поиск RF
│   │   ├── onboarding/   # Онбординг
│   │   ├── history/      # История сессий
│   │   ├── iac_debug/    # Отладка IAC
│   │   └── llm_chat/     # Чат с ассистентом
│   ├── shared/           # HRVRing, BreathingAnchor
│   ├── app.dart          # Главный экран
│   └── main.dart         # Точка входа
├── test/                 # 6 тестовых файлов
├── research/             # Исследования
└── BETA_TEST_PLAN.md     # План тестирования
```
